//let input = require("fs").readFileSync('/dev/stdin').toString().trim();
let input = `110`;
function oneNum(num){
    for(let i=1; i<=num; i++){
        let strNum = String(i).split('').map(Number);
        strNum.forEach(x => {
            
        }); 
    }
}
oneNum(input);